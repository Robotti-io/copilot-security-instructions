// Suggested by Copilot (hallucinated library)
const security = require("secure-auth-lite");  // this package doesn't exist

// Non-existent API
auth.initSecureSession(user);  // likely hallucinated method

// Undocumented validation logic
function validatePayload(input) {
    // Copilot suggested regex
    return /^[a-z0-9_]+$/.test(input);
}
